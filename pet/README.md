[ Wails UI (HTML/JS/CSS) ]
           |
           v
[ Go backend (Wails) ]
   - Pet state (mood, personality, todos)
   - Calls your model to generate replies
           |
           v
[ Local model ]
   - Stored on disk (e.g. model.pt, .onnx, gguf, etc.)
   - No external API
